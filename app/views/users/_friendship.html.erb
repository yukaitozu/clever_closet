<!-- if friend  -->
<% if current_user.friends.include?(user) %>
  <p>Already Friends</p>
  <%= form_for :user, url: user_remove_friend_path(user) do |f| %>
      <%= f.submit "Remove Friend", class: "btn btn-red btn-request" %>
  <% end %>
<% elsif current_user.requested_friends.include?(user)%>
  <p>you recive a friend request</p>
<% elsif current_user.pending_friends.include?(user)%>
  <p>requested</p>
<% else %>
  <%= form_for :user, url: user_request_friendship_path(user) do |f| %>
      <%= f.submit "Request Friend", class: "btn btn-red btn-request" %>
  <% end %>
<% end %>







