<!-- things to show -->
<!-- friends request -->
<!-- looks suggetion -->

<% Notification.where(user: current_user).each do |notification|%>
  <%= notification.notify_type %> <p>from</p><%= User.find(notification.actor_id).username %>
  <%= notification.target.title %>
  <br><br><br>
<% end %>
