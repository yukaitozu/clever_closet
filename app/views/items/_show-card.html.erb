<div class="card-polaroid">
  <% if @item.photo.attached? %>
    <%= cl_image_tag @item.photo.key, height: 700, width: 600, crop: :fill, background: "none", class: "card-show" %>
  <% else %>
    <%= image_tag "blouse.jpg", class: "card-show" %>
  <% end %>
</div>

