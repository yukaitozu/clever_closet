
  <% if @item.photo.attached? %>
    <%= cl_image_tag @item.photo.key, height: 600, width: 500, crop: :fill, class: "card-show" %>
  <% else %>
    <%= image_tag "blouse.jpg", class: "card-show" %>
  <% end %>

