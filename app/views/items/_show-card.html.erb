
  <% if @item.photo.attached? %>
    <%= cl_image_tag @item.photo.key, height: 250, width: 300, crop: :fill, class: "card-show" %>
  <% else %>
    <%= image_tag "blouse.jpg", class: "card-show" %>
  <% end %>

