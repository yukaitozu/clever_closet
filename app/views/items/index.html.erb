<h1>My Closet</h1>

<%= render 'searchbar_items'%>

<% if @items.any? %>
  <% @items.each do |item| %>
    <%= render "items/cards", item: item %>
  <% end %>
<% else %>
  <p>No results in your closet</p>
<% end %>

<% if @friends_items %>
<p>Found in your friends closets:</p>
  <% @friends_items.each do |item| %>
    <%= render "items/cards", item: item %>
  <% end %>
<% end %>
