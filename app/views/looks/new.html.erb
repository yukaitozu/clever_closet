<h1>Create a New Look</h1>

<%= simple_form_for :looks do |f| %>
  <%= f.input :title %>
  <%= collection_check_boxes(:look, :item_ids, @items, :id, :name) %>
  
  <%= f.submit "Create", class: "btn btn-red" %>
<% end %>